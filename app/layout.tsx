import type { Metadata } from "next";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "KPF Academy — Kinetic Pro Fitness Academy",
  description: "Excellence in Fitness Education. Mumbai's premier fitness academy with 17 years of experience. Programs in strength, nutrition, wellness and more.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
